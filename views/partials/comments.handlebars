<div class='comments'>
  {{#each this}}
    <div>
      {{!-- 
      this
       --}}
      <p class='card-body2'>
        {{commentContent}}
        -
        {{user.username}}
        {{format_date dateCreated}}
      </p>
    </div>
  {{/each}}
</div>