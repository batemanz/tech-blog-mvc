<div class="card">
  <div class="card-header  flex-row justify-space-between">
    <h2>
      <a href="/post/{{post.id}}">
        <div class="col-8">{{post.postTitle}}</div>
      </a>
    </h2>
    <div class="col-3">posted by {{post.user.username}} on {{format_date post.dateCreated}}
    </div>
  </div>  
  {{!-- 
this
 --}}
  <p class="card-body">
   {{post.postContent}}
  </p>
</div>
<input type="hidden" name="postId" value="{{post.id}}"/>
<div class="card">
  <div class="card-header"></div>
  <form class="card-body" id="newComForm">
    {{!-- 

 --}}
    <p class="form-label">Comment:</p>
    <textarea class="form-input" id="commentBody" name="commentBody"></textarea>
    <button type="submit" class="btn btn-primary">Submit:</button>
  </form>
  {{!-- 
this
 --}}
{{> comments post.comments}}
</div>
<script src="/js/comment.js"></script>